openai>=1.0.0
chromadb>=0.5.0
python-dotenv>=1.0.0

# Streamlit : ancienne UI locale (app.py), conservée pour tests rapides.
streamlit>=1.30.0

# FastAPI : nouvelle API consommée par le frontend certif-academy (api.py).
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# Auth phase 3 : validation locale ES256 du JWT Supabase. PyJWKClient
# (intégré à pyjwt) fetch le JWKS public et le cache 1h. L'extra [crypto]
# installe le package `cryptography` requis pour ES256 (NIST P-256).
pyjwt[crypto]>=2.8.0

# Phase 4 : appel REST à Supabase pour la gate "5/5 chapitres" (RPC
# is_chatbot_unlocked). httpx est déjà tiré comme sous-dépendance par
# openai SDK, on le re-déclare explicitement pour ne pas dépendre du hasard.
httpx>=0.27.0
